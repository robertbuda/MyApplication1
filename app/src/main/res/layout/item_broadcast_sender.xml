<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    ><TextView
    android:id="@+id/TextABoutBroadcast"
    android:text="Broadcast Sender Page"
    android:gravity="center_horizontal"
    android:layout_width="match_parent"
    android:layout_height="wrap_content" />

    <Button
        android:layout_marginBottom="8dp"
        app:layout_constraintBottom_toBottomOf="parent"
        android:id="@+id/buttonBroadcastSender"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Send Broadcast To See Toast"
         />

    <Button
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@id/TextABoutBroadcast"
        android:id="@+id/buttonBroadcastAfterTime"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Send Broadcast To Open Activity After 10 sek"
        />
</android.support.constraint.ConstraintLayout>